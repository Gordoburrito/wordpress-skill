# Legacy local config file (unused, keep ignored to avoid leaking credentials)
config/target-main.sh

# OS / editor
.DS_Store
*.swp
*~

# generated runtime API responses
runtime/

# pulled schema JSON (local working copies from API pull)
wp-content/acf-json/
